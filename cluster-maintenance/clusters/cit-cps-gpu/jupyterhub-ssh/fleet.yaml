name: jupyterhub-ssh
defaultNamespace: jupyterhub
helm:
  chart: ./chart
  releaseName: jupyterhub-ssh
  valuesFiles:
  - values.yaml
diff:
  comparePatches:
  - apiVersion: v1
    kind: Service
    name: jupyterhub-ssh
    operations:
    - {"op": "remove", "path": "/status"}
