name: descheduler
defaultNamespace: descheduler

labels:
  app: descheduler
  name: descheduler

helm:
  chart: descheduler
  repo: https://kubernetes-sigs.github.io/descheduler/
  version: 0.33.0
  releaseName: descheduler
  createNamespace: true
  valuesFiles:
    - values.yaml
