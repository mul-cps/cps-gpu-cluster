name: ingress-nginx
defaultNamespace: ingress-nginx

labels:
  app: ingress-nginx
  name: ingress-nginx

helm:
  chart: ingress-nginx
  repo: https://kubernetes.github.io/ingress-nginx
  version: 4.11.3
  releaseName: ingress-nginx
  valuesFiles:
  - values.yaml

# Depends on MetalLB being ready
dependsOn:
- selector:
    matchLabels:
      name: metallb
