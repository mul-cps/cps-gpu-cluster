name: reflector
defaultNamespace: kube-system
helm:
  repo: https://emberstack.github.io/helm-charts
  chart: reflector
  version: 10.0.2
  # Let's check if the user has a specific policy on versions. No.
  # I will use a specific version if I can find it, but the doc chunk didn't list one.
  # I'll stick to just repo and chart for now, or check if I can 'helm search'
  # Since I can't helm search easily without updating repos, I'll rely on the fleet bundle to fetch.
  # Using structure from user's other files.
labels:
  app: reflector

targets:
  - name: cit-cps-gpu
    clusterSelector:
      matchLabels:
        name: cit-cps-gpu
