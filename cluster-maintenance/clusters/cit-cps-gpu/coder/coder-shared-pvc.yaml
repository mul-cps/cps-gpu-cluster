apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: coder-shared-storage
  namespace: coder
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 500Gi
