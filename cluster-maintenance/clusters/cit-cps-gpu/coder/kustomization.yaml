apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: coder

resources:
  - postgresql.yaml
  - coder-db-secret.yaml
  - coder-shared-pvc.yaml
  - dask-rbac.yaml
  - coder-rbac.yaml
  - coredns-custom.yaml
  - coder-oidc-secret.yaml

